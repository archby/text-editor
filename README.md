# LightWrite

LightWrite is an extremely lightweight (2.7 KiB) text editor made in Python using Tkinter. Since Tkinter is included in Python by default, LightWrite has no requirements.

*Installation:*
```bash
git clone https://github.com/yourusername/text-editor.git
cd text-editor
python main.py
```
